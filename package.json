{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "@powershell if (Test-Path dist) { Remove-Item -Recurse -Force dist } && cd ../phonebook-frontend && npm run build && @powershell Copy-Item dist -Recurse ../phone-fullstack",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"initial code for the backend and the frontend \" && git push",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
